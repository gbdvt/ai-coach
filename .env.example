# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Single-tenant user id (optional; must match existing rows if you change it)
# HYBRID_COACH_USER_ID=

# Coach LLM — only used for classified "question" messages (logs use rules)
OPENAI_API_KEY=
# Optional model override (default: gpt-4o-mini)
# COACH_OPENAI_MODEL=gpt-4o-mini
